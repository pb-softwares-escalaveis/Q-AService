spring:
  config:
    activate:
      on-profile: local
  cloud:
    openfeign:
      client:
        config:
          auctionService:
            url: http://localhost:8080
          user-service:
            url: http://localhost:8081
eureka:
  client:
    enabled: false
